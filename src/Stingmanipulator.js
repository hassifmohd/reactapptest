export default class Stingmanipulator {

    uppercase( originalString) {
        return originalString.toUpperCase();
    }
    
    lowercase( originalString) {
        return originalString.toLowerCase();
    }
}